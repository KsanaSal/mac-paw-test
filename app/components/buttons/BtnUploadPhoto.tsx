const BtnUploadPhoto = () => {
    return (
        <button
            type="button"
            className="flex items-center justify-center pt-[14px] pb-[12px] px-[30px] text-[12px] text-white hover:text-primaryDark bg-primaryDark hover:bg-primaryLight rounded-[10px] leading-[16px] uppercase"
        >
            Upload photo
        </button>
    );
};

export default BtnUploadPhoto;
